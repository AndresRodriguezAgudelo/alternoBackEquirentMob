import { config } from "dotenv";
config();

const {
  DATABASE_HOST,
  DATABASE_USER,
  DATABASE_PASSWORD,
  DATABASE_PORT,
  DATABASE_NAME,
  APP_PORT,
  ACCOUNT_AZURE,
  ACCOUNT_KEY,
  BUCKET_NAME,
  API_PREFIX,
  NODE_ENV,
  TWILIO_KEY,
  TWILIO_CL,
  EMAIL_PASS,
  EMAIL_USER,
  RUNT_USER,
  RUNT_PASS,
  RUNT_NIT,
  RUNT_REAZON_SOCIAL,
  JWT_SECRET,
} = process.env;

export default () => ({
  DATABASE_HOST,
  DATABASE_USER,
  DATABASE_PASSWORD,
  DATABASE_PORT,
  DATABASE_NAME,
  APP_PORT,
  ACCOUNT_AZURE,
  ACCOUNT_KEY,
  BUCKET_NAME,
  API_PREFIX,
  NODE_ENV,
  TWILIO_KEY,
  TWILIO_CL,
  EMAIL_PASS,
  EMAIL_USER,
  RUNT_USER,
  RUNT_PASS,
  RUNT_NIT,
  RUNT_REAZON_SOCIAL,
  JWT_SECRET,
});
